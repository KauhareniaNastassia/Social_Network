import React from 'react'
import css from './Forum.module.css'

type ForumPropsType = {

}

export const Forum = (props: ForumPropsType) => {
    return (
        <div className={css.content}>
            Forum
        </div>
    )
}